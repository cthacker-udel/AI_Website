import { CheckersPlayer } from "../enums/CheckersPlayer";

/**
 * Calculates the board's heuristic given the board and the current turn
 *
 * @param board - The current board instance
 * @param currentPlayer - The current player who's turn it is
 */
export const getBoardHeuristic = (
    board: Board,
    currentPlayer: CheckersPlayer,
): number => {};
